output: dist/
releases:
  - name: deb-release
    jobs:
      - name: release-linux-deb
        package:
          platform: linux
          target: deb
          args:
            build-args:
              - --release
              - --target-platform=linux-x64
            package-args:
              - --deb-config=debian/debian.yml
